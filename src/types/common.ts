export type SectionItemType = {
    navn?: string,
    tidsperiode: string,
    tittel: string,
    beskrivelser: string[],
    lokasjon: string,
    url?: string
}

export type FooterItem = {
    navn: string
}